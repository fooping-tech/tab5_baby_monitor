# Working agreements

- Read PLANS.md before editing and update evidence/blockers before handoff.
- Keep this AGPL research application separate from tab5_rtsp_logger. Repository separation alone does not establish a legal boundary for combined firmware.
- Never commit identifiable child images, recordings, credentials, model binaries or private dataset paths.
- Never call person detection baby identification, or absence of detections proof of an empty room.
- Errors/stale input must produce unknown; posture is unknown until separately implemented and validated.
- Host tests, IDF build, device flash, live camera inference, model accuracy and endurance are distinct evidence.
- Do not flash devices, upload datasets or merge PRs without explicit authorization.
- Run `cmake -S . -B build && cmake --build build && ctest --test-dir build --output-on-failure` and `python3 -m unittest discover -s tests -p 'test_*.py'` for relevant changes.
